"""
Task 1: Password Match Check

Create a variable `password` and prompt the user for a password.
Create another variable `confirmed_password` and prompt the user to re-enter the password.

Check if `password` matches `confirmed_password`.
If it matches, print "Password matched"; otherwise, print "Password mismatch".
"""
